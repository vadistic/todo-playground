datasource db {
  provider = "sqlite"
  url      = "file:../temp/dev.db"
}

generator client {
  provider = "prisma-client-js"
}

model Task {
  id      Int     @default(autoincrement()) @id
  name    String
  content String?
}

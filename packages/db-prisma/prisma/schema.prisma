datasource db {
  provider = "sqlite"
  url      = "file:../temp/dev.db"
}

generator client {
  provider = "prisma-client-js"
}

model Task {
  id        Int      @default(autoincrement()) @id
  createdAt DateTime
  updatedAt DateTime
  name      String
  content   String?
  finished  Boolean
}

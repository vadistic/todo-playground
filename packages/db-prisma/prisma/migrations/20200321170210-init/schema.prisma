datasource db {
  provider = "sqlite"
  url = "***"
}

generator client {
  provider = "prisma-client-js"
}

model Task {
  id      Int     @default(autoincrement()) @id
  name    String
  content String?
}
